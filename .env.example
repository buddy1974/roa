# Site origin used for canonical URLs, JSON-LD schema, and citation links.
# No trailing slash. Required for production SEO metadata to be correct.
# In development this may be left empty — pages still render correctly.
VITE_SITE_URL=https://your-domain.com
